{{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="button">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
<a class="button" href="{{ path('app_account_manage_courts') }}">Retour à la gestion des courts </a>